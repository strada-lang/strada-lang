func make_adder(int $n) scalar {
    return func (int $x) int { return $x + $n; };
}

func main() int {
    my int $i = 0;
    while ($i < 100) {
        my scalar $add = make_adder($i);
        my int $result = $add->(10);
        $i = $i + 1;
    }
    return 0;
}
