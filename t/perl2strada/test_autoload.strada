# Converted from Perl: t/perl2strada/test_autoload.pl
# Generated by perl2strada (multi-pass converter)
# NOTE: Lines marked REVIEW: need manual type checking
# NOTE: Lines marked TODO: need manual conversion

#!/usr/bin/perl
# use strict;  # Not needed in Strada
# use warnings;  # Not needed in Strada

package DynamicClass;

# TODO: AUTOLOAD not supported in Strada

# TODO: DESTROY not needed in Strada (use reference counting)

package main;

func AUTOLOAD(scalar ...@_) dynamic {
    my ($self) = @_;
    our scalar $AUTOLOAD;
    my scalar $method = $AUTOLOAD;
    $method =~ s/.*:://;
    if ($method eq 'DESTROY') { return; }
    die("Method " . $method . " ! found");
}
func DESTROY(scalar ...@_) dynamic {
    my ($self) = @_;
    # Clean up resources
    say("Destroying object\n");
}

func main() int {
    my scalar $obj = bless {}, "DynamicClass";
    return 0;
}
